# Argo Frontend — Environment Variables
# Copy to .env.local and fill in values.

# Backend API URL — NEXT_PUBLIC_ prefix exposes to client-side code
NEXT_PUBLIC_API_URL=http://localhost:8000

# Site gate password — server-side only (no NEXT_PUBLIC_ prefix)
SITE_PASSWORD=argo-dev
